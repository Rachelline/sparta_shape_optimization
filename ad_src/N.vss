# VSS collision model parameters for the single-species N.species deck
# used by ad_src/ (matches tools/ad_verify/N.species; parameter values
# for atomic N taken from examples/custom/air.vss).

# species  diameter(m)  omega  tref(K)  alpha

N   3.0E-10   0.80   273.15   1.0
